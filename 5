#include <stdio.h>

int main()
{
    char y;

    printf("Enter any char: ");
    scanf("%c",&y);



     if (y == 'a'||y == 'e'||y == 'i'||y == 'o'||y == 'u')
        printf("%c is  vowel.", y);
    else
        printf("%c is not  vowel.", y);

    return 0;
}
